{
  "name": "IBM Cloud Code Engine Deployments",
  "slug": "ibm-cloud-vercel",
  "description": "Deploy Vercel projects to IBM Cloud Code Engine and report status via Vercel Checks.",
  "homepage": "https://github.com/your-org/ibmcloudvercel",
  "support": "https://github.com/your-org/ibmcloudvercel/issues",
  "categories": [
    "deployment",
    "monitoring"
  ],
  "settings": [
    {
      "key": "INTEGRATION_BASE_URL",
      "description": "Public base URL of the integration backend, e.g. https://integration.example.com",
      "required": true
    },
    {
      "key": "VERCEL_WEBHOOK_SECRET",
      "description": "Shared secret used to verify x-vercel-signature on webhook requests.",
      "required": true,
      "encrypted": true
    },
    {
      "key": "IBM_CLOUD_API_KEY",
      "description": "IBM Cloud API key for authenticating with IBM Cloud services.",
      "required": true,
      "encrypted": true
    },
    {
      "key": "IBM_CODE_ENGINE_PROJECT_ID",
      "description": "Code Engine project ID where the application will be deployed.",
      "required": true
    },
    {
      "key": "IBM_CODE_ENGINE_IMAGE_REFERENCE",
      "description": "Target container image reference (e.g. docker.io/user/app:latest).",
      "required": true
    },
    {
      "key": "IBM_REGISTRY_SECRET",
      "description": "Code Engine registry secret name for pushing the built image.",
      "required": true
    }
  ],
  "scopes": {
    "team": [
      "read",
      "projects:read",
      "deployments:read",
      "checks:write"
    ],
    "project": [
      "read",
      "checks:write"
    ]
  },
  "events": [
    "deployment.created"
  ],
  "lifecycle": {
    "install": "/integration/install",
    "update": "/integration/update",
    "uninstall": "/integration/uninstall",
    "webhook": "/integration/webhook"
  },
  "notes": [
    "Replace base URLs and support links with your production values.",
    "Confirm scopes against the current Vercel Integration schema before publishing."
  ]
}
